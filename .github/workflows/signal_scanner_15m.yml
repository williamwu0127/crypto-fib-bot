name: 15m Signal Scanner

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Run 15m Scanner & Auto Trader
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.CRYPTO_WEBHOOK_URL }}

        run: |
          python signal_scanner_15m.py
